# AI 政策板块 · 政策分析 / 政策深度 / 政策研报

> 生成依据：`sources.analyze_policy()` 对当日真实抓取标题的统计与推演
> 状态：**已落地为产品板块**。推送简报与页面均按
> `AI 每日总结 → AI 看盘 → AI 情绪热力图 → AI 政策分析 → AI 政策深度 → AI 政策研报 → AI 板块机会 → 全网快讯`
> 的顺序排列，即「AI 看盘」紧跟「AI 每日总结」，「AI 政策分析」排在「AI 看盘」「AI 情绪热力图」之后。
> 本文档是板块口径说明；页面为静态预览，实时结果见 `GET /api/policy` 与每日推送。

---

## 〇、板块与实现落点

| 位置 | 内容 |
|---|---|
| `policy_impact.py` · `analyze_policy_market_impact()` | **「AI 政策深度」主问题**：近 30 日已核验的全球政策发布对 A股 / 港股 / 美股的影响。指数分可复算；已观测与机制推演分开 |
| `sources.py` · `analyze_policy()` | 政策引擎：8 个政策维度热度 + 多空方向 + 鹰鸽取向 + 支撑新闻；始终附带 `market_impact`。`deep=True`（默认）再叠加下面 ②-⑦ 的对照层 |
| `sources.py` · `_POLICY_KB` / `retrieve_policy_context()` / `policy_knowledge()` | ①政策法规知识库（RAG）：检索历史政策 → 与新信号对比分析 |
| `sources.py` · `interpret_policy_modifiers()` | ③政策修饰词的经济学 + 法学双维解读（`_POLICY_MODIFIERS` 术语库） |
| `sources.py` · `analyze_policy_sentiment()` | ④政策舆情情感打分（正向 / 中性 / 负向 + 官媒与市场的预期差） |
| `sources.py` · `policy_chain_graph()` | ⑤政策传导图谱（政策主体 → 行业 → 产业链上下游节点） |
| `sources.py` · `analyze_policy_reasoning()` / `policy_mindmap()` / `policy_research_note()` | ②四步分析师推理链 + 思维导图 + 结构化政策影响研报 |
| `sources.py` · `lstm_policy_forecast()` | ⑥纯 Python 单层 LSTM + BPTT：政策后中长期走势节奏 |
| `sources.py` · `prophet_policy_decompose()` | ⑦Prophet 式加性分解（趋势 + 季节 + 政策效应），剥离季节看政策窗口波动 |
| `sources.py` · `get_policy_series()` / `set_policy_klines()` | 模型输入序列：推送路径缓存的真实日 K → 明确标注的合成演示序列兜底 |
| `sources.py` · `build_html()` | 推送卡片：「AI 政策分析」+「AI 政策深度」+「AI 政策研报」，排在「AI 每日总结 → AI 看盘 → AI 情绪热力图」之后 |
| `index.html` | 页面板块 `AI POLICY` / `AI POLICY DEEP` / `AI POLICY RESEARCH`（在 `AI WATCH` 看盘、`AI HEATMAP` 热力图之后） |
| `server.py` | `GET /api/policy`（`?deep=0` 不跑模型，仍返回近30日 `market_impact`） |
| `test_sources.py` | `PolicySectionTest` / `PolicyMarketImpactTest` / `PolicyKnowledgeBaseTest` / `PolicyModifierTest` / `PolicySentimentTest` / `PolicyGraphTest` / `PolicyReasoningTest` / `PolicyModelTest` / `PolicyDeepWiringTest` |
| `test_server.py` | `GET /api/policy` 响应结构 + 推送内容含政策三张卡片且位置在「AI 板块机会」之前 |

口径：政策维度标签见 `sources._POLICY_BUCKETS`；鹰鸽词库见 `_HAWKISH` / `_DOVISH`（否定前缀
`_POLICY_NEG_NEAR` / `_POLICY_NEG_FAR` 会把立场票翻转，如「不再那么鸽派」计为鹰派）。

---

## 〇.五、近 30 日全球政策发布 → 股市影响

这是「AI 政策深度」卡片的主问题，不是当日标题词频。实现见 `policy_impact.py`。

- **窗口**：含当日的近 30 个自然日。截止 2026-09-23 时，窗口是 2026-08-25 至 2026-09-23。
- **计入什么**：台账里已核验发文日的正式发布、政策讲话（信号）和窗口内生效。8 月 19 日宣布、9 月 9 日才实施的美债回购，窗口内只记「生效」，不把发文再计一次。
- **不计入什么**：没有核实日期的快讯。当日抓取标题只用来标注「仍在定价」，不改变指数分，也不被写成新的政策发布。
- **三市**：A 股、港股、美股分开给指数分和受益 / 承压板块。指数分 = 各主题同向组的最强贡献 + 0.22×方向×(同向条数-1)。阈值：≥1.2 偏多，≥0.45 中性偏多，±0.45 内中性，≤-1.2 偏空。
- **已观测 vs 机制**：只有公开收盘报道才标已观测。例如 2026-09-16 美联储加息 25bp 至 3.75%-4.00% 后，道指收跌约 600 点。A 股、港股没有对应的已复核点位，就只写传导机制。
- **诚实缺口**：窗口截止日晚于最新已核验发文时，写明缺口，不把空白日补成行情结论。

对照层（知识库检索、术语口径、舆情、传导图谱、推理链）仍排在这张卡片的后部，不替代窗口内发文。`GET /api/policy?deep=0` 也会返回 `market_impact`。

---

## 一、政策法规知识库外掛（RAG）

解读新政策时**先检索、再对比**，而不是让模型凭印象作答：

- **库内容**（`_POLICY_KB`，18 条，覆盖全部 8 个政策维度）：央行货币政策执行报告、
  9·24 一揽子增量政策、超长期特别国债与化债方案、大规模设备更新行动方案、「人工智能+」行动意见、
  新「国九条」与减持新规、中央金融工作会议、美联储降息与缩表、欧洲央行 / 日本央行决议、
  地产金融政策、两用物项出口管制条例、民营经济促进法、BIS 对华管制规则等。
  每条内部仍含：发文机关、时间、文件类型、关键表述、背景摘要、历史市场含义与受影响行业；
  这些字段供检索与模型判断使用，**AI 政策简报只展示日期、机关、文件类型、标题与匹配度**，
  不展示关键表述、背景摘要、历史市场含义或逐条对比文本。新增一条只需在列表里加一行，
  `dimension` 与 `_POLICY_BUCKETS` 标签对齐即可挂上当日维度。
- **检索打分**（`retrieve_policy_context()`，口径写死、可复算）：关键词命中 +2、
  当日政策维度匹配 +3、表述命中 +1.5；同分取更新的政策。内部返回 `score` / `matched` / `why`，
  但对外简报不展示逐条检索依据。
- **对比分析**（`policy_knowledge()` → `comparison`）：把当日鹰鸽取向与历史条目的取向对齐后给出
  「同向 / 反向 / 背景基准」三种解读句式，例如：历史同为收紧取向 → 合规成本与估值同时受压，
  题材与高估值先调整、龙头与高分红相对抗跌。

---

## 二、模拟高级分析师的推理链（含研报与思维导图）

四步拆解，每步都带证据（当日政策新闻 + 知识库历史政策），置信度由跨源命中数与样本量决定
（≥6 源 & ≥10 条 & ≥6 条信号 = 高；≥3 条 = 中；否则低）：

1. **宏观背景**：政策热度维度 + 鹰鸽取向 + 修饰词术语口径 + 历史政策背景
2. **行业限制**：政策净空维度 + 知识库中的制度约束（管制 / 从严 / 禁止类条款）
3. **资金流向**：政策面多空比分 + 资金偏向与回避方向 + 舆情窗口判断
4. **受益板块**：政策净多维度 + 传导链上的行业节点 + 知识库条目的受益行业

在此基础上自动生成：

- **思维导图**（`policy_mindmap()`）：根节点 = 当日政策主线，8 个分支（宏观背景 / 政策工具与术语口径 /
  行业限制 / 资金流向 / 受益与承压板块 / 产业链传导 / 政策窗口期 / 模型视角）。
- **结构化政策影响研报**（`policy_research_note()`）：结论评级（政策利好 / 承压 / 中性）、
  时间尺度（短期 1-4 周 · 中期 1-6 个月 · 长期 6-36 个月）、摘要、四步拆解、受益与承压方向、
  **长远冲击评估**（每个受影响行业给出 结构性 vs 周期性、时间尺度与传导路径）、风险提示、政策窗口期。

---

## 三、金融术语口径（修饰词解读）

对标「国内金融巨头基于开源模型微调的金融专有大模型」的术语理解，用 `_POLICY_MODIFIERS` 术语库
（30+ 条）对政策文本中的修饰词做**经济学 + 法学双维**解读，`strength` 为 -3（收紧 / 强约束）…+3（宽松 / 强支持）：

| 修饰词 | 政策语义 | 强度 | 市场含义 |
|---|---|---|---|
| 适度宽松 | 货币政策基调转松（2024-12 中央经济工作会议后的口径） | +2.0 | 降准降息预期升温，长久期成长与高股息同时受益 |
| 稳健 / 稳健的货币政策 | 不搞强刺激、留有余地 | 0.0 / +0.5 | 不预期大水漫灌，行情依赖结构性工具与财政配合 |
| 择机（降准降息） | 落地时点不确定 | — | 市场对时点反复定价，预期本身成为波动来源 |
| 适度加力 / 更加积极 | 财政扩张 | +1.5 / +2.0 | 订单与需求可见度改善，周期与顺周期受益 |
| 从严 / 强监管 | 执法尺度与违规成本同步抬升 | -1.5 | 小市值与题材承压，龙头相对受益（法学：从严执法宣示） |
| 严禁 / 不得 | 禁止性规范 | -2.0 / -1.5 | 相关业务预期归零，替代方向受益（法学：强制性禁止规范） |
| 不搞大水漫灌 | 抑制总量宽松预期 | -1.0 | 压制流动性驱动的估值扩张 |
| 原则上 / 依法依规 / 试点 | 执行弹性与合规约束 | -0.5 / -0.5 / +0.5 | 实际影响小于字面（法学：例外许可、程序合法性） |

长词优先、短词若被长词包含则不重复计权（「适度宽松」命中后不再单算重叠短词）。
术语口径与鹰鸽取向不一致时会显式提示（例如新闻以管制表述为主、而引用的政策原文口径偏松），
不掩盖分歧。对外简报在「政策术语口径（修饰词解读）」下只显示一条加权强度与方向汇总句，不列逐项修饰词、市场含义或法律含义。

---

## 四、政策舆情情感打分与政策窗口期

- **统计范围**：命中政策维度关键词的舆情，**或来自官方信息源**（国务院 / 部委 / 央行 / 境外监管机构，
  官媒解读本身就是政策舆情）的条目。
- **打分口径**：正向词与负向词命中差值定极性，「坚决 / 严厉 / 罕见 / 史上最大 / 大幅」等强度词把极性加倍；
  聚合成 正向 / 中性 / 负向 计数 + 归一化情绪分（-1…1）。
- **官媒 vs 市场**：官媒 / 官方口径与市场化媒体解读分开统计，情绪差 `gap` ≥ 0.3 即判定为
  **政策窗口期 · 预期差**；同向偏正 = 情绪加速期，同向偏负 = 政策避险期，其余为观望期。
  对外只说明情绪差与统计性质，不补充历史市场解释，并固定附带「仅供参考、不作为投资依据」。

---

## 五、政策传导图谱

按政策维度预置传导链（`_POLICY_CHAINS`，覆盖全部 8 个维度），当日命中哪个维度就把对应的链拉出来，
并带上该维度当日的政策净向：

```
白宫 / 商务部 / 外交部（政策主体）
  └─ 半导体设备、存储芯片、军工、能源航运（受影响行业）
       上游：出口管制清单、关税税率、关键资源供给
       中游：供应链改道与转口、国产替代进度、库存与交期
       下游：终端成本、海外资本开支、风险偏好与避险资产
```

输出 `chains`（含 `nature` 结构性 / 周期性 与 `horizon` 时间尺度）、`nodes` 与 `edges`，可直接用于图形化渲染。

---

## 六、LSTM 与 Prophet 式模型

两个模型都是**纯 Python 实现、零第三方依赖**，输入统一为 `{name, source, dates, closes}`：

**LSTM（`lstm_policy_forecast()`）**——擅长捕捉时间序列的非线性规律与长期依赖，用于评估
突发重大政策（降息、行业监管）后股价 / 汇率的中长期走势节奏：

- 日收盘价 → 日收益率 → z-score 归一化 → 长度 5 的滑窗；单层 LSTM（3 个隐藏单元，每单元独立的
  输入 / 遗忘 / 输出门与候选记忆），BPTT 反向传播 + 梯度裁剪（阈值 5.0）+ SGD（学习率 0.08 线性衰减），
  固定种子 `_POLICY_LSTM_SEED` 保证可复现；窗口数 > 20 时自动降到 30 轮，控制推送链路耗时。
- 以最后一窗状态递归外推 10 个交易日，给出前 3 日 / 4-7 日 / 全程累计、拐点位置、动能衰减或延续。
- **政策情景**：`shock` 为归一化的情景输入偏移（偏鸽 +、偏鹰 -，由鹰鸽取向 + 修饰词强度合成），
  与基线路径之差即「政策情景相对基线的偏移」。输出统一标注为**模型情景、非预测**。
- 样本不足 12 根日 K 时**不外推**，如实写出数据缺口。

**Prophet 式加性分解（`prophet_policy_decompose()`）**——趋势 + 季节性 + 政策突变，
用于分析政策出台后的中期波动并剥离季节因素：

- 模型：`log(close) = 截距 + 线性趋势 + 二次趋势 + 变点铰链 + 周度傅里叶项 + 政策事件哑变量 + 残差`，
  岭回归（正规方程 + 高斯消元）拟合；变点仅在样本 ≥30 根日 K 时启用（与趋势项高度共线）。
- 输出：样本内趋势总量与当前斜率、周度季节振幅、去季节化残差波动、拟合 R²、
  **政策效应**（回归口径 + 事件研究口径两套，永久性水平位移会被趋势 / 变点吸收，故同时给出
  事件研究口径）、**政策窗口内 / 外波动倍数**、10 日去季节化外推路径。
- 样本不足 10 根日 K 时不分解；样本窗口未覆盖知识库政策事件时明确标注「政策效应系数不可辨识」。

**序列来源**：`collect_market_for_push()` 抓到的日 K 会缓存给模型层复用（`set_policy_klines()`）；
无实时行情时回退**确定性合成序列**（`source="synthetic"`，趋势 + 周度季节 + 一个政策跳变日），
页面与推送都会明确标注「合成演示序列（无实时行情）」，不与真实行情混淆。
`POLICY_SERIES_LIVE=1` 可让 `get_policy_series()` 显式打一次行情接口。

---

## 七、数据来源与更新说明

- 政策统计与深度层全部来自当日 `collect_all()` 的真实抓取标题（网络不可用时回退内置演示数据）；
  行情序列来自主备行情接口，离线时用合成演示序列兜底。
- 当日样本没有政策信号时，各层如实标注（「暂无政策面信号可统计」「知识库检索未启动」
  「传导图谱未展开」「LSTM 不外推」「政策效应系数不可辨识」），不编造结论。
- 推送容量分档：政策深度层按 `level 2 全量 → 1 精简（去掉思维导图与背景长文）→ 0 整段省略` 收敛，
  快讯列表在每档内先从每源 3 条收敛到 1 条，整段省略是最后一步；四个主分析栏目始终保留。
- 数据仅供参考，不构成投资建议；政策细则以官方原文为准。

---

*仅供参考，分析研究 · 作者：章鱼 ai*
